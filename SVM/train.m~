function [Model] = train( Model)
%UNTITLED8 Summary of this function goes here
%   Detailed explanation goes here
    for i=1:1
        x=Model.TrainFeatures;
        t=Model.TrainLabel;
        t(t~=1)=-1;
        N=size(x,1);
        K=x*x(
    end

end

